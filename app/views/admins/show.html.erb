<div class="slice slice-lg section-rotate slice-lg masthead">
    <section class="pt-5 pt-lg-0 bg-transparent">
        <div class="container">
            <div class="row">

                <%= render partial: 'layouts/card_dashboard' %>
            </div>
        </div>
    </section>
	<h3>Do you want to change your Photo ?</h3>
	<%= form_tag admin_photo_admins_path(@admin), multipart: true do %>
	  <%= file_field_tag :photo_admin %>
	  <%= submit_tag "Update" %>
	<% end %>
</div>